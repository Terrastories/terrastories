<div id="map" class="ts-MainMap"></div>
<div class="ts-Overlay">
  <h1><%= t('map', locale: :srm)%></h1>
  <!-- to display the available languages uncomment line below -->
  <%#= I18n.available_locales %>
  
  <% if current_user %>
    <p><%= current_user.email %></p>
    <p><%= link_to "log out", destroy_user_session_path, method: :delete %></p>
  <% else %>
      <p><%= link_to "log in", new_user_session_path %></p>
  <% end %>
</div>

<%= render(partial: "home/stories.json", locals: {stories: stories}) %>

<%= react_component('Card', render(partial: "home/stories.json")) %>
